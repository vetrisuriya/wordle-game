import {describe,it,expect} from 'vitest';import {dailyWord,isValidWord,scoreGuess} from '../core/utils/gameEngine';import {english} from '../features/languages/english';
describe('game engine',()=>{it('generates deterministic daily word',()=>{expect(dailyWord(english,5,new Date('2024-01-01'))).toBe(dailyWord(english,5,new Date('2024-01-01')))});it('validates words',()=>{expect(isValidWord(english,'crane',5)).toBe(true);expect(isValidWord(english,'zzzzz',5)).toBe(false)});it('scores guesses',()=>{expect(scoreGuess('crane','crane',english).every(x=>x.state==='correct')).toBe(true)})});
